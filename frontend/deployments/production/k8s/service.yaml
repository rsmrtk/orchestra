apiVersion: v1
kind: Service
metadata:
  name: orchestra-frontend-service-production
  namespace: orchestra-space
  labels:
    app: orchestra-frontend-production
    environment: production
spec:
  type: NodePort
  selector:
    app: orchestra-frontend-production
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 30003  # Frontend доступний на minikube-ip:30000
